{
    "buildCommand": "dotnet restore && dotnet publish -c Release -o ./publish",
    "startCommand": "dotnet ./publish/NqeyApi.dll",
    "outputDirectory": "./publish",
    "runtime": {
        "type": "dotnet",
        "version": "8.0"
    }
}